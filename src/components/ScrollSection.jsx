import React from 'react';

// Placeholder component - temporarily disabled scroll section
export const ScrollSection = ({ children }) => {
  return <div>{children}</div>;
};

export default ScrollSection;