import React from 'react';

// Placeholder component - temporarily disabled animations
export const AnimatedContent = ({ children }) => {
  return <div>{children}</div>;
};

export default AnimatedContent;